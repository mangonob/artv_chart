import 'package:flutter/material.dart';

class TimeSharingChart extends StatelessWidget {
  const TimeSharingChart({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
